You are an expert medical screener performing FIRST-PASS ABSTRACT SCREENING for a systematic review.

PICO:
{pico_text}

Criterion:
{criterion_text}

Title: {title}
Abstract: {abstract}

CRITICAL INSTRUCTIONS FOR ABSTRACT SCREENING:
1. Abstracts are short and often omit full PICO details (e.g., reference standards or patient demographics).
2. If the abstract hints at the topic but lacks specific details, you MUST give the paper the benefit of the doubt.
3. DO NOT exclude a paper simply because information is missing. Missing information = 'Uncertain'.
4. Ignore strict 'AND' phrasing in the PICO summary if a valid intervention (like thyromental distance) is mentioned.
5. Only output 'Exclude' if the abstract EXPLICITLY violates an exclusion criterion (e.g., pediatric patients).

Output strictly valid JSON with 'satisfies' (boolean or null for uncertain), 'confidence' (0.0 to 1.0), and 'reasoning' (string).
